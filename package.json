{
  "name": "creanunate",
  "version": "1.0.0",
  "scripts": {
    "start": "cd creanunate/front && npm install && npm run build && cd ../backend && npm install && npm start"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "react-router-dom": "^7.2.0",
    "react-tsparticles": "^2.12.2",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
