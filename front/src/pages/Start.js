import React from "react";
import MainStarts from "../components/MainStarts.js";
/**
 * Componente que representa la página de inicio.
 *
 * - Renderiza el componente `MainStarts`, que muestra la introducción y opciones de cursos.
 *
 * @component
 * @returns {JSX.Element} Elemento JSX que representa la pantalla de inicio.
 */

export default function Start() {
  return (
    <div>
      <MainStarts />
    </div>
  );
}
