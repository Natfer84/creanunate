import React from "react";
import Login from "../components/Login.js";
/**
 * Componente que representa la página de inicio de sesión.
 *
 * - Renderiza el componente `Login`, que contiene el formulario de acceso.
 *
 * @component
 * @returns {JSX.Element} Elemento JSX que representa la página de login.
 */

export default function Login() {
  return (
    <div>
      <Login />
    </div>
  );
}
